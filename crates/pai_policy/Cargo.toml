[package]
name = "pai_policy"
description = "Policy evaluation engine for PAI-Kernel · OPA/Rego decision layer via regorus (pure-Rust, no external server) for PAI-CD v2.2 constitutional rules"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true

[dependencies]
regorus = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
