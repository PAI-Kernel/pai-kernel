[package]
name = "pai_evasion"
version = "0.1.0"
edition = "2021"
description = "MP-1 — Anti-Evasion Detection for PAI-CD"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
