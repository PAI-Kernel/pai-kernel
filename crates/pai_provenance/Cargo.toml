[package]
name = "pai_provenance"
version = "0.1.0"
edition = "2021"
description = "Doc 11 — Supply Chain & Artifact Provenance for PAI-CD"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
hex = "0.4"
time = { version = "0.3", features = ["serde", "formatting", "parsing"] }
