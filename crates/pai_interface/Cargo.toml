[package]
name = "pai_interface"
description = "Shared types and validation schema for PAI-Kernel governance daemon · enforces growth-signal denylist on KernelContext per PAI-CD v2.2"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
thiserror = "2"
