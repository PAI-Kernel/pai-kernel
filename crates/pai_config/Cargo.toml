[package]
name = "pai_config"
description = "Configuration schema and loader for PAI-Kernel governance daemon · parses pai-kernel.toml with defaults and structural validation"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3"
