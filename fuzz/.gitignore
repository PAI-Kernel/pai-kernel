# cargo-fuzz generated artifacts (NOT committed · grow with each run)
artifacts/
target/

# corpus/ tracked for seed inputs · libFuzzer mutates from these to explore.
# Adopters cloning repo get representative seeds for each target ·
# bootstrap fuzzer coverage faster.
# Note: cargo-fuzz adds to corpus/ during CI runs · they may be committed
# (corpus growth helps regression coverage) OR purged periodically.
